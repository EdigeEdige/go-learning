## День 1 — 07.07.2026
Что понял: установил go version go1.26.1 darwin/arm64
package main и func main оба обязаны существовать и оттуда запускается программа
Println выводит текст и переводит курсор на следующую строку
Printf также функция для вывода но дает в параметрах удобно записать значение переменных
import "fmt" пакет для вывода и формата данных

Повторить: verbs форматирования (%s, %d, %.2f, %v), целочисленное деление (10/3 = 3), оператор % (остаток).

## День 2 — 07.07.2026
Что понял: формы объявления (var, :=, const), zero value, явная конвертация типов, ловушка целочисленного деления и int(0.5) → 0, операторы / и % как пара, обмен a, b = b, a, нетипизированные константы, math.Pi.
Что повторить: глаголы Printf (%d, %.2f, %s, %T) — завтра пригодятся; и следи за именами переменных, это было главное замечание дня (number, courseOfDollar, circleRadius).

