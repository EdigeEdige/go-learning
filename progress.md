## День 1 — 07.07.2026
Что понял:
установил go version go1.26.1 darwin/arm64
package main и func main оба обязаны существовать и оттуда запускается программа
Println выводит текст и переводит курсор на следующую строку
Printf также функция для вывода но дает в параметрах удобно записать значение переменных
import "fmt" пакет для вывода и формата данных

Что повторить: 
verbs форматирования (%s, %d, %.2f, %v), целочисленное деление (10/3 = 3), оператор % (остаток).

## День 2 — 07.07.2026
Что понял:
формы объявления (var, :=, const), zero value, явная конвертация типов, ловушка целочисленного деления и int(0.5) → 0, операторы / и % как пара, обмен a, b = b, a, нетипизированные константы, math.Pi.

Что повторить: 
глаголы Printf (%d, %.2f, %s, %T) — завтра пригодятся; и следи за именами переменных, это было главное замечание дня (number, courseOfDollar, circleRadius).

## День 3 — 08.07.2026
Что понял:
if/else: без скобок вокруг условия, { на той же строке, условие только bool
Цепочки else if: порядок от сильного условия к слабому
Guard clause: ошибочный случай отсекается первым, без вложенности
switch: авто-break, default обязателен по смыслу, вариант без выражения для bool-веток
Встроенная max(), оператор %
Именование: функция = глагол, переменная = существительное

Что повторить:
if с коротким statement (if v := ...; cond) — сегодня в коде не использовал, а это будущий if err != nil
Область видимости переменных в блоках

## День 4 — 09.07.2026
Что понял:
Все три формы for в Go (классическая, как while, бесконечная с break).
Идиоматичный паттерн early-exit через continue/break без лишнего else.
Паттерн "разбор числа на цифры" через % 10 и / 10 — применил его в двух разных задачах (реверс и сумма цифр), что говорит о том, что паттерн действительно усвоен, а не просто скопирован.
Идею оптимизации проверки простоты числа до √n — разобрался, почему это работает, через доказательство от противного.

Что повторить:
Off-by-one ошибки на границах циклов (< vs <=) — тебе не раз приходилось это подмечать самому или после моего вопроса. В субботнем режиме (день 7) стоит специально обратить на это внимание.
Идиома "избегать else после break/continue/return" — держи в голове на будущее, при ревью собственного кода.


